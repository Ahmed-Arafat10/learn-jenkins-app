pipeline {
  agent any
  stages {
    stage('Build Custom AWS CLI Docker Image') {
      steps {
        sh '''
          docker build -f nightly-jenkins/custome-aws-cli/Dockerfile -t custom-aws-cli .
        '''
      }
    }
  }
}
